export interface GalleryImage {
  src: string;
  alt: string;
}

/**
 * Pendiente: el usuario va a elegir manualmente qué fotos del local van acá.
 * Agregar los archivos a /public/images/galeria/ y completar este array.
 */
export const galleryImages: GalleryImage[] = [];
